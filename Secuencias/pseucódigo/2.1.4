Se dispone de una secuencia de números de socios, y se desea saber la cantidad de socios que están registrados.

ACCION cant_socios ES
    AMBIENTE
        s, cont: entero
        sec: secuencia numérica

    PROCESO
        ESC("Este algoritmo cuenta la cantidad de socios registrados mediante sus numeros en una secuencia.")

        ARR(sec)
        cont:= 0

        MIENTRAS NFDS(sec)
            AVZ(sec, s)
            cont:= cont + 1
        FIN_PROCESO

        CERRAR(sec)

        ESC("Hay ",cont," socios registrados.")
FIN_ACCION