Crear un procedimiento que intercambie los valores de dos variables numéricas.

ACCION cambio_valores ES
    AMBIENTE
        var1, var2, var_auxiliar: real

        PROCEDIMIENTO cambio_valores_var(a, b: real) ES
            AMBIENTE
                var_auxiliar_a := real

            var_auxiliar_a := a
            a := b
            b := var_auxiliar_a
            ESC("El nuevo valor de su variable 1 es: ",a)
            ESC("El nuevo valor de su variable 2 es: ",b)
        FIN_PROCEDIMIENTO

    PROCESO
        ESC("Este algoritmo recibe dos valores, los guarda en variables, y luego, intercambia los valores.")
        ESC("Ingrese el valor para la variable 1: ")
        LEER(var1)
        ESC("Ingrese el valor para la variable 2: ")
        LEER(var2)

        ESC("Variable 1: ",var1)
        ESC("Variable 2: ",var2)
        cambio_valores_var(var1, var2)
FIN_ACCION
